---
description: Abre a TUI do CDD para visualizar o understanding.json de uma feature
---

Execute o comando abaixo no terminal e informe o resultado ao usuário:

```bash
cd ~/cdd && cdd $ARGUMENTS
```

Se der erro, verifique se o usuário está na pasta correta do projeto e se a feature existe em `.cdd/$ARGUMENTS/understanding.json`.
